import { Theme, webDarkTheme, webLightTheme } from '@fluentui/react-components';

export const atlasLightTheme: Theme = { ...webLightTheme };
export const atlasDarkTheme: Theme = { ...webDarkTheme };
export const excelTheme: Theme = { ...webLightTheme };
